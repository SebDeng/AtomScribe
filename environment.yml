name: AtomScribe
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - ffmpeg  # Required for pydub MP3 encoding
  - pip:
    - PySide6>=6.6.0
    - pydantic>=2.0
    - loguru>=0.7.0
    - sounddevice>=0.4.6
    - numpy>=1.24.0
    - pydub>=0.25.1
    - faster-whisper>=1.0.0  # Real-time speech-to-text
    - llama-cpp-python>=0.2.0  # LLM post-processing (install with CUDA: CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python)
